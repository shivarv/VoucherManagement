<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_Expired__c</fullName>
    <description>if expiry date is greater than today  ,checkbox is checked</description>
    <externalId>false</externalId>
    <formula>IF(  AND(NOT(ISBLANK(Credit_Expiry_Date__c )),  Credit_Expiry_Date__c &lt;  TODAY() ) , true, false)</formula>
    <inlineHelpText>is the voucher expired?</inlineHelpText>
    <label>Is Expired ?</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
